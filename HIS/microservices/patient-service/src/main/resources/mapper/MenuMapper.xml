<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.hospital.patients.menu.mapper.MenuMapper">

  <select id="findHierarchy" resultType="kr.co.hospital.patients.menu.dto.MenuFlatDto">
    SELECT
      menu_id AS id,
      parent_id AS parentId,
      code,
      name,
      path,
      icon,
      sort_order AS sortOrder
    FROM menu
    WHERE is_active = 1
    START WITH parent_id IS NULL
    CONNECT BY PRIOR menu_id = parent_id
    ORDER SIBLINGS BY sort_order, menu_id
  </select>
</mapper>
