# final_his

Hospital information system project bundled as a single public repository.

## Structure

- `HIS/` - backend/microservice sources
- `frontend/` - Next.js frontend sources

## Public repository notes

Database dump files, local environment files, dependency folders, build outputs, logs, and IDE metadata are intentionally excluded.
Use local environment variables or local-only config files for real credentials.
